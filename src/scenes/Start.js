// src/scenes/Start.js
export class Start extends Phaser.Scene {
    constructor() {
        super('Start');
    }

    create() {
        // This is just a placeholder scene that immediately transitions
        // The actual game selection happens in gameSelector.js
    }
}